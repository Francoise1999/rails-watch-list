<div class="container">
  <h1 class="mt-5 mb-5" >New Bookmark</h1>

  <%= simple_form_for [@list, @bookmark] do |f| %>
    <%= f.input :comment %>
    <%= f.association :movie,   as: :radio_buttons %>
    <%= f.button :submit, class: "btn btn-success mb-5" %>
  <% end %>
</div>
